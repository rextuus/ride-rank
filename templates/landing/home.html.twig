{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ encore_entry_link_tags('compare') }}
{% endblock %}

{% block body %}
    <div class="text-center mb-4">
        <h2 class="fw-bold">Welche Achterbahn ist besser?</h2>
        <p class="text-muted">Tippe auf eine Karte oder überspringe den Vergleich</p>
    </div>

    {{ component('CoasterCompare') }}

    <!-- Why Compare kommt hier extra -->
    <div class="mt-5 text-center text-muted small">
        <p class="fw-bold mb-3">Warum vergleichen?</p>
        <div class="d-flex justify-content-center gap-5">
            <div>
                <i>{{ ux_icon('solar:ranking-line-duotone', {style: 'height: 2.5em; color: #3a2a1a'}) }}</i>
                <div>Ranking</div>
            </div>
            <div>
                <i>{{ ux_icon('codicon:collection', {style: 'height: 2.5em; color: #3a2a1a'}) }}</i>
                <div>Sammlung</div>
            </div>
            <div>
                <i>{{ ux_icon('iconoir:community', {style: 'height: 2.5em; color: #3a2a1a'}) }}</i>
                <div>Community</div>
            </div>
        </div>
    </div>

{% endblock %}
